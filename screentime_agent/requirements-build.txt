# Requirements for building standalone executables

# Core dependencies
supabase>=2.0.0

# Build tool
pyinstaller>=6.0.0

# macOS GUI (menu bar icon)
rumps>=0.4.0; sys_platform == 'darwin'

# Windows GUI (system tray icon)  
pystray>=0.19.0; sys_platform == 'win32'
pillow>=10.0.0; sys_platform == 'win32'
